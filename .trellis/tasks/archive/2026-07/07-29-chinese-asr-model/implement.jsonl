{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Documents the required guarded-import pattern for optional GPU deps (transcription/__init__.py try/except ImportError around whisperx-dependent re-exports). transcriber.py will gain a top-level `import funasr`, which must fail-safe the same way; confirm no separate guard is needed since __init__.py already wraps the whole `from .transcriber import ...` line."}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "Defines this project's one-exception-class-per-stage convention (TranscribeError already the transcription module's class) and the worker's per-episode try/except-and-mark-FAILED pattern that LocalFunASR/LanguageRoutingTranscriber must follow for consistency with LocalWhisperX."}
{"file": ".trellis/spec/backend/directory-structure.md", "reason": "Describes the transcription/ package's existing stage-module layout (transcriber.py / worker.py / __init__.py) so new classes land in transcriber.py rather than new files, matching the documented pattern."}
